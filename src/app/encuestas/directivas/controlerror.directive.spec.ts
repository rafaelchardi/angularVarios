import { ControlerrorDirective } from './controlerror.directive';

describe('ControlerrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlerrorDirective();
    expect(directive).toBeTruthy();
  });
});
